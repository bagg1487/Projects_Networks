# Отчет по Docker
Неоптимизированный (один stage, python:3.11) - 1.14 GB
Оптимизированный (multi-stage, python:3.11-slim) - 147 MB


### Количество слоев и их описание

#### Оптимизированный образ (147 MB) - 6 слоев:

| № | Слой | Размер | Описание |
|---|-------------------------------------------|--------|---------------------------------------------------|
| 1 | `FROM python:3.11-slim`                   | 78.6 MB| Базовый образ (минимальный)                       |
| 2 | `COPY requirements.txt .` + `pip install` | 23 MB  | Установка зависимостей                            |
| 3 | `COPY . .`                                | 13.3 kB| Копирование кода приложения                       |
| 4 | `RUN addgroup/adduser`                    | 4.32 kB| Создание непривилегированного пользователя        |
| 5 | `USER appuser`                            | 0 B    | Переключение на непривилегированного пользователя |
| 6 | `EXPOSE 8105` + `CMD`                     | 0 B    | Метаданные контейнера                             |

#### Неоптимизированный образ (1.14 GB) - 4 слоя:


| № | Слой                      | Размер | Описание                                             |
|---|---------------------------|--------|------------------------------------------------------|
| 1 | Базовый слой Debian       | 120 MB | Базовый образ Debian                                 |
| 2 | apt-get update (пакеты 1) | 60.2 MB| Установка системных пакетов                          |
| 3 | apt-get update (пакеты 2) | 185 MB | Установка дополнительных пакетов                     |
| 4 | apt-get update (пакеты 3) | 656 MB | Установка основных пакетов (включая gcc, make и др.) |
| 5 | apt-get update (пакеты 4) | 17.8 MB| Дополнительные утилиты                               |
| 6 | Скачивание Python         | 64 MB  | Загрузка архива Python                               |
| 7 | `RUN pip install`         | 40.8 MB| Установка Python-зависимостей                        |
| 8 | `COPY . .`                | 250 B  | Копирование кода (мало, т.к. код маленький)          |
| 9 | `EXPOSE` + `CMD`          | 0 B    | Метаданные                                           |


### Команды сборки и запуска

#### Оптимизированный образ:
```bash
# Сборка
docker build -t week10-app .

# Запуск
docker run -d -p 8105:8105 --name profiles-container week10-app

# Проверка работы
curl http://localhost:8105/
curl http://localhost:8105/health

# Просмотр логов
docker logs profiles-container

# Остановка и удаление
docker stop profiles-container
docker rm profiles-container